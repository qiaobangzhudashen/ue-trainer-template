# 新游戏 / 新会话侦察表

> 用途：每个新游戏或新目标类目开工时复制一份填写。先记录证据，再写代码。
> 状态标记：`[ ] 未查`　`[?] 待验证`　`[x] 已确认`

## 0. 基本信息

- 游戏：
- 游戏版本 / EXE 文件版本：
- 游戏路径：
- MOD 目标功能：
- 目标类目：`物品 / 装备 / 货币 / 属性 / 技能 / 任务 / 其他`
- 侦察日期：
- 反编译目录：
- 当前结论：`[ ] 仍缺前置证据  [ ] 可开始最小探针  [ ] 可开始业务实现`

## 1. 运行时与工程

### 构型证据

- `[ ]` Likely Mono：证据（`*_Data/Managed`、程序集等）：
- `[ ]` Likely IL2CPP：证据（`GameAssembly.dll`、`global-metadata.dat` 等）：
- `[ ]` Unknown：仍需确认的证据：
- BepInEx 分支 / 版本：
- 插件基类：
- Unity 版本：
- TargetFramework：
- 逻辑程序集或 interop 来源：
- csproj：
- 参考模板或可运行插件：

### 目录检查

- `[ ]` 游戏 EXE
- `[ ]` `*_Data/Managed`
- `[ ]` `GameAssembly.dll`
- `[ ]` `*_Data/il2cpp_data/Metadata/global-metadata.dat`
- `[ ]` `BepInEx/core`
- `[ ]` `BepInEx/interop`
- `[ ]` 反编译输出

## 2. 启动与日志

- 启动方式：`[ ] 直接 EXE  [ ] Steam  [ ] 启动器  [ ] 其他`
- Steam AppID / 启动命令：
- 启动前置条件：
- 游戏是否自退：`[ ] 否  [ ] 是  [?] 未验证`
- BepInEx 日志位置：
- Unity / 游戏日志位置：
- `[ ]` BepInEx 日志已生成
- `[ ]` 日志已出现插件 GUID
- `[ ]` 已确认游戏版本与目标版本一致

## 3. 输入系统

- 输入来源：`[ ] Legacy Input  [ ] Input System  [ ] Rewired  [ ] 游戏自定义  [ ] 混合  [ ] 未知`
- 证据（DLL、类型或反编译调用链）：
- 输入入口 / 适配方式：
- 游戏原有快捷键占用：
- MOD 快捷键：
- `[ ]` 快捷键无冲突
- `[ ]` 已实测按键触发
- 备注：

## 4. 生命周期与就绪条件

- 插件 `Awake` 证据：
- 驱动入口：`[ ] 游戏常驻 Update  [ ] OnGUI  [ ] Harmony Patch  [ ] 其他`
- 常驻宿主类 / 方法：
- 是否清理外部 `MonoBehaviour`：`[ ] 否  [ ] 是  [?] 未验证`
- `Update` 是否执行：
- `OnGUI` 是否执行：
- 场景切换后是否仍执行：
- 玩家对象就绪条件：
- 背包 / 属性 / 数据表就绪条件：
- 可安全调用的最早时机：
- 需要等待或重试的条件：

## 5. 目标系统六问

### 1. 谁承载它？

- 类 / 命名空间：
- 实例入口：
- 字段 / 属性全集：
- 本功能实际使用字段：
- 明确不用的字段：

### 2. 有哪些维度 / 档位？

- 枚举或数值维度：
- 品级 / 颜色 / 强化等语义：
- 名称来源：
- 颜色来源：
- 子类型：
- 前置条件：
- 叠加上限或唯一性：

### 3. 怎么读？

- 入口对象：
- 完整方法签名：
- 返回值语义：
- 当前值 / 原生参数：
- 读取时的就绪条件：

### 4. 怎么写？

- 完整方法签名：
- 参数含义：
- `[ ]` 绝对设置　`[ ]` 增量修改　`[ ]` 未确认
- 默认值 / 最小值 / 上限：
- 特殊参数语义：
- 官方调用参照：

### 5. 写完怎么生效？

- 缓存重算：
- 事件：
- UI 刷新：
- 场景限制：
- 是否需要再次读取：

### 6. 官方如何产出同类数据？

- 参照系统：`打造 / 掉落 / 奖励 / 学习 / 交付 / 刷新 / 其他`
- 入口方法：
- 调用链：
- 合法参数组合：
- 随机字段 / 状态字段生成规则：

## 6. 读—改—写记录

- 读取基准值 / 原生参数：
- 只修改的最小字段：
- 若是增量 API：`diff = 新值 - 基准值`
- 写入前置检查：
- 写入后重新读取结果：
- UI / 缓存刷新结果：
- 异常与日志：

## 7. 最小验证

- `[ ]` 插件被扫描且无加载异常
- `[ ]` 输入能触发且无冲突
- `[ ]` 前置对象未就绪时不崩溃
- `[ ]` 读取值与游戏界面一致
- `[ ]` 修改后再次读取值正确
- `[ ]` 对象出现在正确 UI
- `[ ]` 对象字段完整且可正常使用
- `[ ]` 重复操作不会重复叠加
- `[ ]` 场景切换后仍正常
- `[ ]` 异常被记录且不影响其他功能
- 验证日志位置：
- 验证结果：

## 8. 部署与回滚

- 构建命令：
- 输出 DLL：
- 部署目录：`<GameRoot>/BepInEx/plugins/`
- `[ ]` 游戏已关闭后复制
- `[ ]` DLL 时间戳已更新
- `[ ]` BepInEx 日志确认加载
- 上一稳定 DLL：
- 配置备份：
- 回滚步骤：
- 回滚后验证：

## 9. 待确认问题

1. 
2. 
3. 
